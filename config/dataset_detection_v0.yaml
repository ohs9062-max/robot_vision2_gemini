# validation_v0 전체 풀을 디텍션 전용으로 사용하는 설정.
# segmentation은 이 데이터셋에서 더미(가짜)이므로 절대 세그멘테이션 학습에 쓰지 않는다 -- 이 config는 train_detection.py 전용이다.
# step/ditch_hole 클래스는 이 데이터셋 15000장 전체에 0개 존재 -- puddle/obstacle만 학습된다.
dataset_root: "/home/hs/rang/robot_vision2_gemini/dataset/validation_v0"
splits_file: "data/splits/validation_v0_det_splits.json"

image_width: 1920
image_height: 1080

dirs:
  images: "images"
  segmentation: "segmentation"
  detection: "detection"
  metadata: "metadata"
